# Optional manual override for unusual network setups.
# Leave EXPO_PUBLIC_API_BASE_URL unset during normal local development so the app
# can auto-resolve the Expo dev host.
EXPO_PUBLIC_API_PORT=3010
